rollovers.lua
=============

rollovers.lua
├─ CustomPhysicsRollovers.reset()
│  └─ reinitializes rollover cache/state
└─ CustomPhysicsRollovers.update(vehicle)
   ├─ refreshRolloverChecks(vehicle, now)         [when cache expires]
   │  └─ getVelocityMagnitude(velocity)
   ├─ getVelocityMagnitude(velocity)              [active-force pass]
   ├─ CustomPhysicsUtil.clamp(...)                [settleProgress]
   └─ ApplyForceToEntity(...)                     [force at center or height offset]

Expanded Local Call Detail
--------------------------

refreshRolloverChecks(vehicle, now)
└─ getVelocityMagnitude(velocity)

getVelocityMagnitude(velocity)
└─ returns speed, velocityX, velocityY, velocityZ
