# setr ars_skip_uptodate_print "0"

# --- Menu ---
# Default key players press to open the vehicle manager menu (Config.menu.defaultKey). Default: F6
# setr vm_default_key "F6"

# --- Nearby panel integration ---
# Distance in meters within which the vehicle manager shows the PI panel for the current vehicle. Mirrors performancetuning setting.
# setr vm_pi_panel_distance "30"

# --- Save ownership ---
# Primary identifier prefix used to resolve who owns a saved vehicle (Config.save.ownerIdentifierPrefixes[1]). Default: license:
# setr vm_owner_id_prefix "license:"
